package se.liu.antos931jakos322.towerdefence.entities;

import java.awt.*;

public class BulletProjectile extends TowerProjectile
{

    public BulletProjectile(final Color color, final double drawScale, final int speed) {
	super(color, drawScale, speed);
    }
}
